# Previews de templates de portfólio

Mesma convenção que `resume/`, com pastas nomeadas pelo `template_key` da API quando `type: portfolio`.

Exemplo:

```
portfolio/
  portfolio_minimal/
    preview-desktop.webp
    preview-mobile.webp
```
