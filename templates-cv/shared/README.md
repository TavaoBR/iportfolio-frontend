# Assets partilhados entre templates

Ícones, padrões de fundo ou referências reutilizáveis que **não** pertencem a um único `template_key`.

Manter aqui apenas o necessário; preferir previews por template em `resume/` ou `portfolio/`.
